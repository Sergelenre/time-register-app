querySnapshot.docs.forEach{
    - fetch dates frpm firestore
    - save as list (List<String>)
    AddDayToMap();
}

AddDayToMap() {
    year, month ,day parse
    and add to map
}

// research 
Date.getDay()